# Tweets — 0.7.1

Use any of these as-is or mix. Build-in-public, same tone as TWEETS_SHIP_IT.md.

---

## 1. Lead (ship + highlights)

promptctl 0.7.1 is out. Pick your default model with `promptctl models --set`, Atlas is in the list, rate an output and we’ll offer one free retry per day. Config: add/remove API keys per provider. Same goal: structure prompts, waste less. https://prompt-ctl.com

---

## 2. Models + ratings

0.7.1: set a default model, try Atlas (our hosted LLM), and if you rate a result low we give you one free retry per day. Ratings help us improve. promptctl — CLI for structured prompts. https://prompt-ctl.com

---

## 3. Config + transparency

You can now add or remove API keys per provider from the CLI. We also made it clear that `create` uses our hosted enhancer (no key required to try). promptctl 0.7.1. https://prompt-ctl.com

---

## 4. Short / punchy

0.7.1: default model, Atlas in the list, ratings + one free retry per day, API key config per provider, site analytics and worker polish. promptctl. https://prompt-ctl.com

---

## 5. One-liner

promptctl 0.7.1: set default model, Atlas in the list, one free retry when you rate low, and API key config per provider. https://prompt-ctl.com

---

## Tags (pick 1–3 per tweet to stay under limit)

- #buildinpublic #indiehacker #CLI #promptengineering #LLM #devtools
- #promptctl #promptengineering #opensource
